<?php
    // PHP empty() function the function can be used to verify the value of a variable.

    // when variable value empty return true otherwise return true
    /* $x ='';
    $y = empty($x);
    var_dump($y); */

    // when array empty return true otherwise return true
    $x = array('HTML','CSS');
    $y = empty($x);
    var_dump($y);
?>